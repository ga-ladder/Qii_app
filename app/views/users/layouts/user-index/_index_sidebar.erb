<div class="user-index__sidebar">
  <h1>
    <i class="fas fa-users"></i>
    ユーザー一覧
  </h1>
  <p class="user-index__component">Qiitaに登録しているユーザーの一覧です。 現在1万人以上のユーザーが登録しています。</p>
  <p class="user-index__index">索引</p>
  <div class="user-index__table">
  <% user_index.each do |p| %>
    <%= link_to users_path(value: p), class:'user-index__item', id: p do %>
      <%= p %>
    <% end %>
  <% end %>
  </div>
</div>
